/**
 * Единая точка входа для навигации
 *
 * Это файл упрощает управление навигацией и позволяет легко переключаться
 * между разными навигационными компонентами без изменения импортов
 * в других частях приложения.
 */

// Экспортируем навигацию для использования в App.tsx
export { AppNavigator as Navigation } from './AppNavigator';

// Экспортируем типы для использования во всем приложении
export * from './types';
